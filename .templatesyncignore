# Files that are permanently specific to this image and should never be
# overwritten by upstream ublue-os/image-template changes.
cosign.pub
image-template.env

# Default GITHUB_TOKEN can't push changes to workflow files; merge upstream
# CI changes here by hand occasionally instead of via this job.
.github/workflows/
